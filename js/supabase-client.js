// Supabase replaced with custom PostgreSQL backend via REST API
// All data operations now go through /api/* endpoints
console.log('✅ Backend: PostgreSQL via Neon + Vercel API routes');

window.supabase = null;
window.supabaseClient = null;
